'use strict';

module.exports = function (str) {
  str = str.toUpperCase();

  if ( str.length < 3 || str.length > 4 ) {
    throw new Error(str + ' does not appear to be a valid airport identifier');
  }

  return str;
};
